






























































creates an eventhandler - when select is changed, the following ajax function will run.

sets up an ajax function.
sets the ajax function's url to 2php/getBorders.php,
sets the type to GET,
sets the data to an object with the key iso with the value of the select countryies value.


sets up the console to log the result as a JSON string if the request is successful.


if the result.status.name is ok,

difines a varible borber and assigns it L.geoJSON function(with takes coordinates as an argument and returns a leaflet layer polygon/polylines) and add it to the map.


calls the function map.fitBounds and assigns it the parameter of border.getBounds, which in plain terms causes the slected country's border to fill up the page. 